package com.sist.lv1_0525;

public class Budget_jun {
	/*
	 *  function solution(d, budget) {
	    let answer = 0;
	    let total = 0;
	    d.sort(function(a,b) {
	        console.log(a,b);
	        return a-b;
	    });
	    for(let i = 0; i < d.length; i++) {
	        if(total + d[i] <= budget) {
	            total += d[i];
	            answer++;
	        }
	    }
	    return answer;
	}

	 *  
	 *  */
}
